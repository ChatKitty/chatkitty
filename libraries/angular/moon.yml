type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    exclude: ['build']

tasks:
  build:
    command: 'ng build chat-ui'
    inputs:
      - '@group(app)'
      - '@group(sources)'
    outputs:
      - 'dist'
