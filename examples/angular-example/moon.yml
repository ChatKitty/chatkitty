type: 'application'

workspace:
  inheritedTasks:
    exclude: ['typecheck']

toolchain:
  typescript:
    disabled: true

fileGroups:
  app:
    - 'src/app/index.html'

tasks:
  serve:
    command: 'ng serve angular-example'
    local: true
