type: 'application'

workspace:
  inheritedTasks:
    exclude: ['typecheck']

toolchain:
  typescript:
    disabled: true

fileGroups:
  app:
    - 'public/index.html'
    - 'vue.config.*'

tasks:
  serve:
    command: 'vue-cli-service serve'
    local: true
